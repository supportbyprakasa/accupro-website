node_modules/
.DS_Store
shots/

# dist/ is generated by build.mjs — CI rebuilds it on every deploy.
# But dist/assets/ is NOT generated: the stylesheet, main.js and the logo are
# hand-maintained source files that happen to live there. Ignore the generated
# output, keep the assets. (See README §4.)
dist/*.html
dist/services/
dist/sitemap.xml
dist/robots.txt
